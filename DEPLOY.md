# 🚀 Инструкция по деплою на продакшен

## 📋 Подготовка перед деплоем

### 1. Конфигурация проекта (ОБЯЗАТЕЛЬНО)

**Основной способ настройки - через интерактивный конфигуратор:**

```bash
npx hasyx config
```

Эта команда:
- Открывает интерактивный интерфейс для настройки всех параметров
- Редактирует `hasyx.config.json` (источник истины)
- Автоматически генерирует `.env` и `docker-compose.yml`
- **⚠️ ВАЖНО:** `.env` и `docker-compose.yml` помечены как auto-generated - НЕ редактируйте их вручную!

### 2. Минимально необходимые настройки для продакшена

В `hasyx.config.json` через `npx hasyx config` нужно настроить:

#### Обязательные секции:

1. **Hasura** (`hasura`):
   - `url` → `NEXT_PUBLIC_HASURA_GRAPHQL_URL`
   - `secret` → `HASURA_ADMIN_SECRET`
   - `jwtSecret` → `HASURA_JWT_SECRET`
   - `eventSecret` → `HASURA_EVENT_SECRET`

2. **Host** (`hosts`):
   - `url` → `NEXT_PUBLIC_MAIN_URL` (публичный URL приложения)
   - `port` → `PORT` (порт для запуска)
   - `basePath` → `NEXT_PUBLIC_BASE_URL` (опционально, для подпутей)

3. **NextAuth Secrets** (`nextAuthSecrets`):
   - `secret` → `NEXTAUTH_SECRET` (обязательно!)
   - `url` → `NEXTAUTH_URL` (обычно = `NEXT_PUBLIC_MAIN_URL`)

#### Опциональные секции (в зависимости от используемых функций):

- **OAuth провайдеры** (Google, Yandex, GitHub, Facebook, VK, Telegram)
- **Firebase** (для push-уведомлений)
- **Resend** (для email-верификации)
- **Storage** (MinIO, S3, etc.)
- **PostgreSQL** (если используется локальная БД)
- **Docker Hub** (для публикации образов)

### 3. Варианты конфигурации (Variants)

Проект поддерживает несколько вариантов конфигурации (dev, prod, vercel, etc.):

```json
{
  "variant": "prod",  // активный вариант
  "variants": {
    "prod": {
      "host": "production",
      "hasura": "production",
      "nextAuthSecrets": "production",
      // ... другие настройки
    }
  }
}
```

**Переключение варианта:**
```bash
npx hasyx config
# Выберите нужный вариант в интерфейсе
```

## 🐳 Деплой через Docker

### Шаг 1: Настройка конфигурации

```bash
# Настройте все параметры через интерактивный конфигуратор
npx hasyx config

# Убедитесь, что выбран правильный вариант (prod)
# Проверьте, что все обязательные переменные настроены
```

### Шаг 2: Сборка Docker образа

**Локальная сборка:**

```bash
docker build \
  -t your-app-name:latest \
  --build-arg NEXT_PUBLIC_HASURA_GRAPHQL_URL="https://your-hasura.hasura.app/v1/graphql" \
  --build-arg NEXT_PUBLIC_MAIN_URL="https://your-app.com" \
  --build-arg NEXT_PUBLIC_BASE_URL="" \
  --build-arg NEXT_PUBLIC_API_URL="https://your-app.com/api" \
  --build-arg NEXT_PUBLIC_BUILD_TARGET="server" \
  --build-arg NEXT_PUBLIC_WS="wss://your-app.com" \
  --build-arg NEXT_PUBLIC_FIREBASE_API_KEY="your-key" \
  --build-arg NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="your-domain.firebaseapp.com" \
  --build-arg NEXT_PUBLIC_FIREBASE_PROJECT_ID="your-project-id" \
  --build-arg NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="your-bucket.appspot.com" \
  --build-arg NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="123456789" \
  --build-arg NEXT_PUBLIC_FIREBASE_APP_ID="1:123456789:web:xxx" \
  --build-arg NEXT_PUBLIC_FIREBASE_VAPID_KEY="your-vapid-key" \
  --build-arg NEXT_PUBLIC_APP_NAME="Your App Name" \
  --build-arg NEXT_PUBLIC_TELEGRAM_BOT_USERNAME="your_bot" \
  .
```

**Или используйте скрипт:**

```bash
./test-docker-build.sh
```

### Шаг 3: Запуск контейнера

**Через Hasyx CLI (рекомендуется):**

```bash
# Создать и запустить контейнер на порту 3000
npx hasyx docker define 3000

# Или на другом порту
npx hasyx docker define 8080

# Просмотр логов
npx hasyx docker logs 3000

# Просмотр переменных окружения
npx hasyx docker env 3000

# Остановка и удаление
npx hasyx docker undefine 3000
```

**Вручную через docker run:**

```bash
docker run -d \
  --name your-app-3000 \
  -p 3000:3000 \
  --env-file .env \
  --restart unless-stopped \
  your-app-name:latest
```

### Шаг 4: Автоматическая публикация в Docker Hub (CI/CD)

Если настроен GitHub Actions workflow (`.github/workflows/workflow.yml`):

1. **Настройте GitHub Secrets:**
   - `DOCKERHUB_USERNAME` - ваш Docker Hub username
   - `DOCKERHUB_PASSWORD` - ваш Docker Hub password/token

2. **Настройте `hasyx.config.json`:**
   ```json
   {
     "dockerhub": {
       "default": {
         "username": "your_dockerhub_username",
         "password": "your_dockerhub_token"
       }
     },
     "variants": {
       "prod": {
         "dockerhub": "default"
       }
     }
   }
   ```

3. **Автоматическая публикация:**
   - Push в `main`/`master` → публикация с тегом `latest`
   - Push тега `v*` → публикация с семантической версией

## 🌐 Деплой на Vercel

### Шаг 1: Настройка конфигурации

```bash
npx hasyx config
# Выберите вариант "vercel" или создайте его
```

### Шаг 2: Настройка Vercel API доступа

Для автоматической синхронизации переменных окружения добавьте в `.env`:

```bash
VERCEL_TOKEN=your_vercel_token
VERCEL_PROJECT_NAME=your-project-name
VERCEL_TEAM_ID=team_xxx  # опционально, для команд
VERCEL_USER_ID=user_xxx  # опционально, для личных аккаунтов
```

**Как получить VERCEL_TOKEN:**
1. Зайдите в [Vercel Settings → Tokens](https://vercel.com/account/tokens)
2. Создайте новый токен
3. Скопируйте токен в `.env`

**VERCEL_PROJECT_NAME:**
- Имя вашего проекта в Vercel (обычно = имя репозитория)

### Шаг 3: Синхронизация переменных окружения

**Автоматическая синхронизация (РЕКОМЕНДУЕТСЯ):**

```bash
npx hasyx vercel sync
```

Эта команда:
- ✅ Читает все переменные из `.env`
- ✅ Удаляет старые переменные в Vercel (production, preview, development)
- ✅ Добавляет все переменные из `.env` в Vercel для всех окружений
- ✅ Автоматически отключает WebSocket для Vercel (`NEXT_PUBLIC_WS=0`)

**Ручная настройка (альтернатива):**

Если не хотите использовать автоматическую синхронизацию, добавьте переменные вручную в панели Vercel:

**Обязательные:**
- `NEXT_PUBLIC_HASURA_GRAPHQL_URL`
- `NEXT_PUBLIC_MAIN_URL`
- `NEXT_PUBLIC_API_URL`
- `HASURA_ADMIN_SECRET`
- `HASURA_JWT_SECRET`
- `HASURA_EVENT_SECRET`
- `NEXTAUTH_SECRET`
- `NEXTAUTH_URL`

**Опциональные (в зависимости от функций):**
- OAuth провайдеры (Google, Yandex, GitHub, etc.)
- Firebase переменные
- Resend API key
- И другие...

### Шаг 4: Деплой

Vercel автоматически использует:
- `buildCommand`: `npm run build` (из `vercel.json`)
- `outputDirectory`: `.next`

Просто сделайте push в подключенный репозиторий.

### Дополнительные команды Vercel

```bash
# Синхронизировать переменные из .env в Vercel
npx hasyx vercel sync

# Очистить все переменные окружения в Vercel (без добавления новых)
npx hasyx vercel clear

# Показать справку
npx hasyx vercel --help
```

## 📦 Деплой статического клиента (GitHub Pages)

### Шаг 1: Сборка клиента

```bash
# Сборка статического клиента
npm run client
```

Эта команда:
- Устанавливает `NEXT_PUBLIC_BUILD_TARGET=client`
- Устанавливает `NEXT_PUBLIC_JWT_AUTH=1`
- Собирает статический экспорт в `client/` директорию

### Шаг 2: Настройка переменных окружения

Для сборки клиента нужны те же переменные, что и для серверной сборки.

### Шаг 3: Деплой

GitHub Actions workflow автоматически:
- Собирает клиент через `npm run client`
- Публикует в GitHub Pages

## 🔧 Команды для продакшена

### Перед деплоем:

```bash
# 1. Настройка конфигурации
npx hasyx config

# 2. Применение миграций БД (если нужно)
npx hasyx migrate

# 3. Генерация схемы Hasura и TypeScript типов
npx hasyx schema

# 4. Синхронизация event triggers
npx hasyx events
```

### Сборка:

```bash
# Для серверного деплоя (Docker, Vercel serverless)
npm run build

# Для статического клиента (GitHub Pages, статический хостинг)
npm run client
```

### Запуск:

```bash
# Production сервер
npm run start

# Или через Docker
npx hasyx docker define 3000
```

## ⚠️ Важные замечания

### 1. Переменные окружения

- **НЕ редактируйте `.env` вручную!** Он auto-generated
- Все изменения делайте через `npx hasyx config`
- `.env` генерируется из `hasyx.config.json`

### 2. Build-time vs Runtime переменные

**Build-time (для Docker build):**
- `NEXT_PUBLIC_*` переменные должны быть переданы как `--build-arg`
- Используются только во время сборки Next.js

**Runtime (для запуска контейнера):**
- Все переменные из `.env` передаются в контейнер
- Включая секреты (HASURA_ADMIN_SECRET, NEXTAUTH_SECRET, etc.)

### 3. Безопасность

- **НЕ коммитьте** `.env` в git (должен быть в `.gitignore`)
- **НЕ коммитьте** `hasyx.config.json` с реальными секретами (используйте варианты)
- Используйте GitHub Secrets / Vercel Environment Variables для CI/CD

### 4. Проверка перед деплоем

```bash
# Проверьте конфигурацию
npx hasyx config

# Проверьте, что все переменные установлены
cat .env | grep -v "^#" | grep "="

# Проверьте сборку локально
npm run build

# Проверьте Docker образ
docker build -t test . && docker run -p 3000:3000 test
```

## 🐛 Troubleshooting

### Проблема: "Missing environment variables"

**Решение:**
```bash
# Проверьте конфигурацию
npx hasyx config

# Убедитесь, что выбран правильный вариант
# Проверьте, что все обязательные секции настроены
```

### Проблема: "Build fails with NEXT_PUBLIC_* not defined"

**Решение:**
- Для Docker: передайте все `NEXT_PUBLIC_*` как `--build-arg`
- Для Vercel: добавьте переменные в панели Vercel
- Для локальной сборки: убедитесь, что `.env` сгенерирован через `npx hasyx config`

### Проблема: "Container won't start"

**Решение:**
```bash
# Проверьте логи
npx hasyx docker logs 3000

# Проверьте переменные окружения
npx hasyx docker env 3000

# Проверьте, что порт свободен
lsof -i :3000
```

## 📚 Дополнительная документация

- **[DOCKER.md](./DOCKER.md)** - Подробная документация по Docker
- **[README.md](./README.md)** - Общая документация проекта
- **[CONTRIBUTING.md](./CONTRIBUTING.md)** - Руководство по разработке

## ✅ Чеклист перед деплоем

- [ ] Запущен `npx hasyx config` и настроены все параметры
- [ ] Выбран правильный вариант конфигурации (prod)
- [ ] Проверен `.env` файл (auto-generated, не редактировался вручную)
- [ ] Применены миграции БД (`npx hasyx migrate`)
- [ ] Сгенерирована схема Hasura (`npx hasyx schema`)
- [ ] Синхронизированы event triggers (`npx hasyx events`)
- [ ] Локальная сборка проходит успешно (`npm run build`)
- [ ] Для Docker: образ собирается и запускается локально
- [ ] Для Vercel: переменные окружения добавлены в панели
- [ ] Healthcheck endpoint работает (`/api/health`)

